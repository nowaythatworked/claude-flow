{
  "name": "flow-rules-evaluator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/cli.ts --outfile ../bin/flow-rules",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "minimatch": "^10.0.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
